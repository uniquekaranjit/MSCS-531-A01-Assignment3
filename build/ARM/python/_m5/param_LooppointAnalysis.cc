/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:330
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/LooppointAnalysis.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "cpu/simple/probes/looppoint_analysis.hh"

#include <vector>
#include "base/types.hh"
#include "base/addr_range.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_LooppointAnalysis");
    py::class_<LooppointAnalysisParams, ProbeListenerObjectParams, std::unique_ptr<LooppointAnalysisParams, py::nodelete>>(m, "LooppointAnalysisParams")
        .def(py::init<>())
        .def("create", &LooppointAnalysisParams::create)
        .def_readwrite("bb_excluded_addr_ranges", &LooppointAnalysisParams::bb_excluded_addr_ranges)
        .def_readwrite("bb_valid_addr_range", &LooppointAnalysisParams::bb_valid_addr_range)
        .def_readwrite("if_listening", &LooppointAnalysisParams::if_listening)
        .def_readwrite("looppoint_analysis_manager", &LooppointAnalysisParams::looppoint_analysis_manager)
        .def_readwrite("marker_valid_addr_range", &LooppointAnalysisParams::marker_valid_addr_range)
        ;

    py::class_<gem5::LooppointAnalysis, gem5::ProbeListenerObject, std::unique_ptr<gem5::LooppointAnalysis, py::nodelete>>(m, "gem5_COLONS_LooppointAnalysis")
        .def("startListening", &gem5::LooppointAnalysis::startListening)
        .def("stopListening", &gem5::LooppointAnalysis::stopListening)
        .def("getLocalBBV", &gem5::LooppointAnalysis::getLocalBBV)
        .def("clearLocalBBV", &gem5::LooppointAnalysis::clearLocalBBV)
        ;

}

static EmbeddedPyBind embed_obj("LooppointAnalysis", module_init, "ProbeListenerObject");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyLooppointAnalysisParamsClass
{
  public:
    gem5::LooppointAnalysis *create() const;
};

template <class CxxClass, class Enable=void>
class DummyLooppointAnalysisShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyLooppointAnalysisShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const LooppointAnalysisParams &>>>
{
  public:
    using Params = LooppointAnalysisParams;
    static gem5::LooppointAnalysis *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyLooppointAnalysisShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const LooppointAnalysisParams &>>>
{
  public:
    using Params = DummyLooppointAnalysisParamsClass;
    static gem5::LooppointAnalysis *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::LooppointAnalysis *
DummyLooppointAnalysisShunt<gem5::LooppointAnalysis>::Params::create() const
{
    return DummyLooppointAnalysisShunt<gem5::LooppointAnalysis>::create(*this);
}

} // namespace gem5

namespace gem5
{
namespace gem5_LooppointAnalysis_create
{

// Base case (no warning)
template<bool>
struct NonDefaultCreate {};

// Specialization with warning for true case
template<>
struct [[deprecated(
        "Warning: LooppointAnalysis is not constructible from LooppointAnalysisParams. "
        "It is deprecated to use non-standard `create()` methods. "
        "If you see this warning followed by a linking error, "
        "the most likely problem is LooppointAnalysis has a parent class with "
        "pure virtual functions.")]] NonDefaultCreate<true> {};

[[maybe_unused]] NonDefaultCreate<
    !std::is_constructible_v<gem5::LooppointAnalysis,
                             const LooppointAnalysisParams &>> warning_instance;
} // namespace gem5_LooppointAnalysis_create
} // namespace gem5
