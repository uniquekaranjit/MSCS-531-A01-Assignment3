/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:330
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/QoSMemSinkInterface.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "mem/qos/mem_sink.hh"

namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_QoSMemSinkInterface");
    py::class_<QoSMemSinkInterfaceParams, AbstractMemoryParams, std::unique_ptr<QoSMemSinkInterfaceParams, py::nodelete>>(m, "QoSMemSinkInterfaceParams")
        .def(py::init<>())
        .def("create", &QoSMemSinkInterfaceParams::create)
        ;

    py::class_<gem5::memory::qos::MemSinkInterface, gem5::memory::AbstractMemory, std::unique_ptr<gem5::memory::qos::MemSinkInterface, py::nodelete>>(m, "gem5_COLONS_memory_COLONS_qos_COLONS_MemSinkInterface")
        ;

}

static EmbeddedPyBind embed_obj("QoSMemSinkInterface", module_init, "AbstractMemory");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyQoSMemSinkInterfaceParamsClass
{
  public:
    gem5::memory::qos::MemSinkInterface *create() const;
};

template <class CxxClass, class Enable=void>
class DummyQoSMemSinkInterfaceShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyQoSMemSinkInterfaceShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const QoSMemSinkInterfaceParams &>>>
{
  public:
    using Params = QoSMemSinkInterfaceParams;
    static gem5::memory::qos::MemSinkInterface *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyQoSMemSinkInterfaceShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const QoSMemSinkInterfaceParams &>>>
{
  public:
    using Params = DummyQoSMemSinkInterfaceParamsClass;
    static gem5::memory::qos::MemSinkInterface *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::memory::qos::MemSinkInterface *
DummyQoSMemSinkInterfaceShunt<gem5::memory::qos::MemSinkInterface>::Params::create() const
{
    return DummyQoSMemSinkInterfaceShunt<gem5::memory::qos::MemSinkInterface>::create(*this);
}

} // namespace gem5

namespace gem5
{
namespace gem5_memory_qos_MemSinkInterface_create
{

// Base case (no warning)
template<bool>
struct NonDefaultCreate {};

// Specialization with warning for true case
template<>
struct [[deprecated(
        "Warning: QoSMemSinkInterface is not constructible from QoSMemSinkInterfaceParams. "
        "It is deprecated to use non-standard `create()` methods. "
        "If you see this warning followed by a linking error, "
        "the most likely problem is QoSMemSinkInterface has a parent class with "
        "pure virtual functions.")]] NonDefaultCreate<true> {};

[[maybe_unused]] NonDefaultCreate<
    !std::is_constructible_v<gem5::memory::qos::MemSinkInterface,
                             const QoSMemSinkInterfaceParams &>> warning_instance;
} // namespace gem5_memory_qos_MemSinkInterface_create
} // namespace gem5
