/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:330
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/CpuCluster.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "cpu/cluster.hh"

#include "sim/clock_domain.hh"
#include "sim/voltage_domain.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_CpuCluster");
    py::class_<CpuClusterParams, SubSystemParams, std::unique_ptr<CpuClusterParams, py::nodelete>>(m, "CpuClusterParams")
        .def(py::init<>())
        .def("create", &CpuClusterParams::create)
        .def_readwrite("clk_domain", &CpuClusterParams::clk_domain)
        .def_readwrite("voltage_domain", &CpuClusterParams::voltage_domain)
        ;

    py::class_<gem5::CpuCluster, gem5::SubSystem, std::unique_ptr<gem5::CpuCluster, py::nodelete>>(m, "gem5_COLONS_CpuCluster")
        ;

}

static EmbeddedPyBind embed_obj("CpuCluster", module_init, "SubSystem");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyCpuClusterParamsClass
{
  public:
    gem5::CpuCluster *create() const;
};

template <class CxxClass, class Enable=void>
class DummyCpuClusterShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyCpuClusterShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const CpuClusterParams &>>>
{
  public:
    using Params = CpuClusterParams;
    static gem5::CpuCluster *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyCpuClusterShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const CpuClusterParams &>>>
{
  public:
    using Params = DummyCpuClusterParamsClass;
    static gem5::CpuCluster *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::CpuCluster *
DummyCpuClusterShunt<gem5::CpuCluster>::Params::create() const
{
    return DummyCpuClusterShunt<gem5::CpuCluster>::create(*this);
}

} // namespace gem5

namespace gem5
{
namespace gem5_CpuCluster_create
{

// Base case (no warning)
template<bool>
struct NonDefaultCreate {};

// Specialization with warning for true case
template<>
struct [[deprecated(
        "Warning: CpuCluster is not constructible from CpuClusterParams. "
        "It is deprecated to use non-standard `create()` methods. "
        "If you see this warning followed by a linking error, "
        "the most likely problem is CpuCluster has a parent class with "
        "pure virtual functions.")]] NonDefaultCreate<true> {};

[[maybe_unused]] NonDefaultCreate<
    !std::is_constructible_v<gem5::CpuCluster,
                             const CpuClusterParams &>> warning_instance;
} // namespace gem5_CpuCluster_create
} // namespace gem5
